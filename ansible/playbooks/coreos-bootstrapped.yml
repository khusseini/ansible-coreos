---
- name: Bootstraps ansible on CoreOS
  hosts: coreos
  gather_facts: false
  roles:
  - role: boot-rescue
    delegate_to: localhost

  - role: coreos
    state: installed
    ansible_user: root
    ansible_python_interpreter: /usr/bin/python

  - role: defunctzombie.coreos-bootstrap
